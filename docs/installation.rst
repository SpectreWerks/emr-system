============
Installation
============

At the command line::

    $ easy_install emr-system

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv emr-system
    $ pip install emr-system
